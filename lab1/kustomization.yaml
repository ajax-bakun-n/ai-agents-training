apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - k8s/
  - kagent/
  - gateway-api/

secretGenerator:
  - name: anthropic-api-key
    namespace: kagent
    options:
      disableNameSuffixHash: true
    envs:
      - .env